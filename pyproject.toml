[project]
name = "limbus_automation"
version = "0.1.0"
description = "Automation toolkit for Limbus Company"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
